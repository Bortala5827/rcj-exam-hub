# 新增考试页 / 加题库 · 防错指南（来自国考实战经验）

本仓库用「一套共享渲染器 + 各考试目录独立 manifest」的模式。国考页（`guokao/`）已跑通，
省考 / 事业编 / 教师 / 文职 4 个页曾因引了已删除的旧脚本而**全部白屏**。
本指南把踩过的坑固化下来，照做即可避免重新加题库时再出错。

---

## 一、页面文件结构（每个考试一套）

```
<exam>/                      考试目录，如 guokao / shengkao / shiye / jiaoshi / wenwen
├── index.html               页面入口（复制自 _template/index.html）
├── assets/
│   └── pdf-manifest.js      真题清单（window.RCJ_PDFS = [...]）
└── bishi/                   真题 PDF 按科目分目录
    ├── xingce/  行测  PDF
    ├── shenlun/ 申论  PDF
    └── ...
```

共享文件（所有考试页共用，不要复制）：
- `assets/list.20260815.js` —— 渲染器（题目索引 + 历年真题库）
- `assets/list.20260731.css` —— 样式

>  **白屏根因**：旧版脚本叫 `/assets/list.js`，已被删。任何页面若还写
> `<script src="/assets/list.js">` 或 `<link href="/assets/list.css">` 都会 404  白屏。
> 现在必须用带日期版本号的 `list.20260731.js` / `list.20260731.css`。

---

## 二、新建一个考试页（3 步）

**1. 复制模板**
```
cp _template/index.html  <exam>/index.html
cp _template/assets/pdf-manifest.js  <exam>/assets/pdf-manifest.js
```

**2. 改 `_template/index.html` 里的 3 处**
- `title`：页面大标题，如 `省考`
- `subtitle`：副标题
- `subjectOrder`：科目顺序数组，如 `['行测','申论']`、`['公基','职测']`、
  `['教综','学科']`、`['公共科目','专业科目']`
- `RCJ_QUESTIONS = []`（没有题目索引就留空，别再写【示例】占位题）

> 渲染器已支持**动态标题**：真题说明文案用 `meta.title`，不再写死“国考”，
> 所以省考页会显示“省考真题原卷”，不会串味。

**3. 放 PDF + 生成清单**（见第三节）

完成后该页即有「历年真题库」区块，每张卷子带「在线查看 / 下载」。

---

## 三、加真题 PDF（防手填出错的关键）

**目录约定**：PDF 按科目放进 `<exam>/bishi/<科目>/`，文件名用**全角配对括号 `（）`**。

科目文件夹名  中文分类名（脚本自动映射）：
| 文件夹 | 分类名 | 文件夹 | 分类名 |
|--------|--------|--------|--------|
| xingce | 行测 | gongji | 公基 |
| shenlun | 申论 | zhice | 职测 |
| gonggong | 公共科目 | jiaozong | 教综 |
| zhuanye | 专业科目 | xueke | 学科 |

**生成清单（推荐，别手填）**：
```
python tools/gen_pdfs.py --exam <exam>
# 例：python tools/gen_pdfs.py --exam shengkao
```
脚本会扫描 `<exam>/bishi/` 下所有 PDF，生成 `<exam>/assets/pdf-manifest.js`，
字段固定为 `year / cat / title / file`，杜绝字段名写错。

**如果手填**，务必注意：
- 字段是 **`file`**（相对路径，如 `bishi/xingce/xxx.pdf`），**不是 `url`**。
- `year` 是数字（如 `2025`），渲染器 `escapeHtml` 已对数字做安全处理，不会崩。

---

## 四、部署前必查（国考踩过的坑）

1. **PDF 必须进 git 跟踪**：仓库无 `.gitignore` 排除 PDF，但确认一下
   ```
   git ls-files '<exam>/bishi' | wc -l
   ```
   数量 = 你放的文件数。没跟踪的 PDF 本地能看、线上 404。
2. **文件名括号配对**：国考有 3 个文件名用了「全角左括号 + 半角右括号」（如 `（副省级)`），
   因磁盘同名不报错，但极脆弱。统一用全角配对 `（）`。
3. **共享脚本改版要刷新缓存**：`assets/list.20260815.js` 是共享的。
   改过它之后，所有考试页 `<script>` 上的 `?v=数字` 要 **+1**（如 `?v=2``?v=3`），
   强制浏览器丢弃旧缓存；用户侧按 **`Ctrl+F5`**（Mac `Cmd+Shift+R`）硬刷。
4. **不要留【示例】占位题**：会误导用户以为是真实真题。没数据就 `RCJ_QUESTIONS = []`。

---

## 五、部署后验证

```
# 强制回源（绕过 CDN 缓存）核对字节数，确认线上=本地
curl -s -H "Cache-Control: no-cache" -o /tmp/live.pdf \
  "https://exam.rcj9527.dpdns.org/<exam>/bishi/<科目>/xxx.pdf"
# 对比本地文件大小；再看响应头 cf-cache-status 是否为 EXPIRED（已回源）
```

---

## 六、申论等扫描件水印处理

扫描件水印 baked 在页面图里，无法删对象，用白色矩形遮盖：
```
python tools/clean_shenlun_watermark.py --dir <exam>/bishi/shenlun --y 780
```
（`--y` 为白边起始 y 坐标，正文最低约 794 时取 780 安全；`--dry-run` 先预览。）

---

## 七、子目录 / 城市专区

首页与各考试页都是静态目录，子页（如广东专区）同理：建 `<exam>/<sub>/` 目录，
复制 `index.html` 改 `RCJ_META`，独立 `pdf-manifest.js` + `bishi/` 即可。

---

## 八、网盘（夸克）双轨：超 25MB / 未来题库走网盘

Cloudflare Pages 单文件硬上限 **25MB**，超限会让整个部署上传失败（省考那次《行测》答案解析
29.6MB 就踩过坑）。两种解法：

1. **压到 25MB 内**（PyMuPDF 降 DPI+JPEG，文件名不变），继续走 CF 内联「在线查看」；
2. **放网盘**（夸克，参考站 `kaogong-materials` 的做法）：不进仓库、不占 25MB 额度、
   仓库永不被撑大。

本仓库用**双轨**：已上线的国考/省考真题继续在 CF 内联看；以后新增的、或超 25MB 的，走夸克。

### 数据怎么放

建 `<exam>/assets/pan-manifest.js`，内容：
```js
window.RCJ_PAN = [
  // title 必须与 pdf-manifest.js 里的 title 完全一致，渲染器按 title 合并
  { title: '2027年广东省考《行测》真题', pan: 'https://pan.quark.cn/s/xxxxxx', code: '' },
  // 仅有网盘、本地没有 PDF 的题（纯网盘资源）：补 cat / year
  { title: '2027广东省考冲刺模拟卷（卷一）', cat: '行测', year: 2027, pan: 'https://pan.quark.cn/s/yyyyyy' },
];
```
- `title`：合并键。与 `RCJ_PDFS` 里某条同名  该卡片**同时出现「在线查看」和「网盘领取」**；
  不在 `RCJ_PDFS` 里  作为独立真题条目，只显示「网盘领取」。
- `pan`：夸克分享链接（`https://pan.quark.cn/s/...`）。
- `code`：提取码（夸克一般免提取码，留空字符串即可；百度网盘才需要）。

### 页面怎么引

在 `<exam>/index.html` 里、引入 `list.20260731.js` 之前，加一行（模板已留注释示例，取消注释即可）：
```html
<script src="assets/pan-manifest.js"></script>
```

### 渲染器行为（已支持）

- 卡片按 `cat` 科目分组，**滚动到视口才渲染**（IntersectionObserver 懒加载），搜索时一次性渲染。
- 有 `file`  显示「在线查看 + 下载 PDF」；有 `pan`  显示「网盘领取」；两者可并存。
- 改过共享脚本后，别忘把所有页的 `?v=数字` +1（见第四节第 3 条）。

> 注意：网盘链接可被整包转存，与“传不走”付费策略相反——**只放公开真题当引流钩子，
> 付费定制内容（Anki / HTML 刷题 / AI 点评）绝不外链网盘**。
